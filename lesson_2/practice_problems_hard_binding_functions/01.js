// What method can we use to bind a function permanently to a particular execution context?

// We can use `Function.prototype.bind` method to bind a function to another execution context permanently.